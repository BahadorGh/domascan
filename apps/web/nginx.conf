server {
  listen 80;
  server_name _;

  # SPA routing
  location / {
    try_files $uri /index.html;
  }
  # Removed /api proxy for Koyeb deployment; frontend calls backend directly via baked VITE_API_URL
}
