{
  "name": "domascan",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "prisma"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "format": "prettier -w .",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prisma": "^5.22.0",
    "@prisma/client": "^5.22.0"
  },
  "dependencies": {
    "dotenv": "^17.2.2"
  },
  "pnpm": {
    "overrides": {
      "error-ex": "1.3.2"
    }
  }
}